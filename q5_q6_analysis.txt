================================================================================
Q5: Attempt to Disprove the PDF Post-Processing Hypothesis
================================================================================

The PDF hypothesis states: Legacy coordinates were computed by rendering the Excel
worksheet to PDF, then extracting vector positions (grid lines, cell boundaries)
from the PDF content stream, and normalizing them to 0-1 page ratios.

--- Test 1: Evidence of PDF coordinate extraction code ---
Result from Q3: NO PDF parsing/vector extraction code exists in the codebase.
The only PDF library (PDFtoImage v5.1.0) is used solely for PDF-to-PNG rasterization.
CONCLUSION: No current evidence of PDF coordinate extraction code.
    -- Does NOT disprove the hypothesis; the legacy system may have had separate code --

--- Test 2: Database cache of PDF-extracted coordinates ---
Result from Q1: NO database table stores PDF geometry (bounding boxes, clip regions,
grid line positions, cell boundaries extracted from PDF).
The only position data is in def_cluster (text ratios already normalized).
No 'cache', 'temp', 'geometry', 'vector', 'clip', or 'bbox' columns exist.
CONCLUSION: No evidence of cached PDF measurements.
    -- Does NOT disprove; legacy code may have extracted without caching --

--- Test 3: Background PDF exists BEFORE ratio computation ---
Check: Is there a timestamp showing the PDF was generated before ratios were stored?
Forms with background_image_file: 459
Forms with def_file (Excel) but NO background_image_file: 0

--- Test 4: Timing analysis of oldest forms ---
